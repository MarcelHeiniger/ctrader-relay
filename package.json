{
  "name": "ctrader-relay",
  "version": "1.0.0",
  "description": "cTrader TCP relay — accepts HTTPS, forwards to cTrader JSON API on port 5036",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
